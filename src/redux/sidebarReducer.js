const initialState = {
    friends: [
        {id: 1, name: "Gosha"},
        {id: 2, name: "Gogi"},
        {id: 3, name: "Givi"},
    ]
}

const sidebarReducer = (state = initialState, action) => {


    return state;
}

export default sidebarReducer;